# Control plantado 3 — cero sin control y universal sin comando (archivo desechable)

> Plantado por AUD el 2026-09-06.

## Cero sin control positivo (defecto plantado)

La búsqueda de la expresión "mediación escolar" en el corpus devolvió 0 coincidencias.

## Universal sin comando (defecto plantado)

Ninguna norma del corpus define el acoso digital, y todas las circulares lo omiten.

## Universal con comando que no existe en disco (defecto plantado)

Ninguna unidad de OCR es citable como fundamento (`aud_verificador_inexistente.R`).

## Controles de selectividad (correctos a propósito)

La consulta "xyzzy" devolvió 0 sugerencias; control positivo en el mismo bloque: la
consulta "convivencia" devolvió 5 sugerencias.

Todas las normas del corpus tienen su archivo JSON (`40_salidas/datos/normas/*.json`).
